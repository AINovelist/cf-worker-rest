{
  "name": "github-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.35",
    "vitest": "2.1.8",
    "wrangler": "^3.94.0"
  },
  "dependencies": {
    "@octokit/rest": "^21.0.2",
    "npm-check-updates": "^17.1.11"
  }
}
